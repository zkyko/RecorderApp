# BrowserStack credentials should be configured via Settings UI or environment variables
# This file is for BrowserStack CLI configuration only
# userName: <configure via Settings → BrowserStack>
# accessKey: <configure via Settings → BrowserStack>
projectName: "D365 Auto-Recorder"
buildName: "Recorder Test Build"
CUSTOM_TAG_1: "Playwright"
CUSTOM_TAG_2: "D365"
testObservability: true
browserstackAutomation: true
framework: playwright
playwrightConfigOptions:
  use:
    trace: 'on'
